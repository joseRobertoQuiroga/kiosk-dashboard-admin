import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
//arreglar el tema de los estados de los kiosco en los imformes aregglar el problema de los tamaños en diferentes vistas , arreglar el rpoblema de configuracion jala eternet